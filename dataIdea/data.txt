// TODO: Scene Group — отдельный тип, не Group с id: 0
// - идея: раз у корневой группы нет и не может быть id (не удаляется, не перемещается,
//   не является "элементом" в обычном смысле) — значит, ей не нужно поле id вообще,
//   а не проверка "id == 0" в каждом методе
// - продумать: свой struct (SceneGroup?) с полями name + content,
//   без id, без opacity/is_visibility? (нужны ли они корню — тоже проверить)
// - продумать: как SceneGroup соотносится с GroupElement/Group —
//   она НЕ элемент дерева (не лежит ни в чьём Vec<GroupElement>),
//   а отдельное поле в Document, как обсуждали в варианте А
// - защита от удаления/перемещения: закрыть и в GUI, и на уровне
//   консоли/API — там, где могут дёргать функции напрямую




// TODO: вынести document (Group, Layer, GroupElement, SceneGroup, IdCounter, IdName)
// в отдельную библиотеку (lib crate), а не оставлять модулем внутри основного проекта
// - причина: структура документа не специфична конкретно для этого графического редактора,
//   в другом проекте её тоже можно было бы взять как основу и дописать свою функциональность
// - продумать: как оформить Cargo workspace для этого — либо как отдельный member
//   рядом с основным бинарником (см. ранний разговор про workspace), либо позже,
//   когда структура document устаканится и не будет часто меняться
