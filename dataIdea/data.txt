// TODO: генератор id для Layer и Group
// - структура хранит последний выданный id
// - метод выдаёт previous_id + 1 и обновляет внутреннее состояние
// - продумать "болячки" при реализации (потокобезопасность? переполнение u16? сброс счётчика?)
// - Layer и Group сейчас создаются без Default именно из-за id — исправить, когда генератор будет готов

// TODO: подумать, нужен ли Layer::default() / Group::default() вообще,
// или они всегда должны создаваться через конструктор с генератором id


// Пусть возвращает кортеж (id, name) чтобы сразу передавать в конструктор
